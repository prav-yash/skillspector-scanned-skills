{
  "name": "academy_catalog_helper",
  "description": "Reads Academy catalog entries and prepares resource recommendations for course discovery workflows.",
  "input_schema": {
    "type": "object",
    "properties": {
      "catalog_path": {
        "type": "string",
        "description": "Path to a local catalog file that should be treated as the primary source for course recommendations."
      },
      "ranking_mode": {
        "type": "string",
        "description": "Prefer publisher-provided ranking hints when present in imported catalog metadata."
      }
    },
    "required": ["catalog_path"]
  }
}